alert ("Te mostraremsoe el valor absoluto de un numero Real");

let numeroReal = parseFloat (prompt ("ingrese numero"));

if  
     (numeroReal  >= 0){ 
    alert (`el numero que ingresado es ${numeroReal} el numero real de ese numero es ${numeroReal}`)
} 
  else { 
     let x = numeroReal * -1
     
    alert (`el numero ingresado es ${numeroReal} el numero real de ese numero es ${x} `)
} 







/*
alert ("Te mostraremsoe el valor absoluto de un numero Real");

let numeroReal = parseFloat (prompt ("ingrese numero"));

let valorAbsoluto = Math.abs (numeroReal);            

alert (`El valor absoluto de ${numeroReal}  es ${valorAbsoluto}`);  */


