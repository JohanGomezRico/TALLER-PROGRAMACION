alert ("hola convertiremos los grados celsius a fahrenheit");

let gradosCelsius = parseFloat (prompt ("ingrese grados celcius"));

let fahrenheit =  (gradosCelsius * 9/5) + 32

alert (`el valor de grados celsiul es ${gradosCelsius}°C
    la convercion a grados faren fahrenheit es ${fahrenheit}°F `)
