alert ("quieres saber el area de un tiangulo");

let base = parseFloat (prompt("digite la base del triangulo"));

let altura = parseFloat (prompt("digite la altura del triangulo"));

let area = ((base * altura)/2);

alert (
     `la base del triangulo es ${base}
     la altura del triangulo es ${altura}
        
     el AREA del triangulo es ${area}`);
