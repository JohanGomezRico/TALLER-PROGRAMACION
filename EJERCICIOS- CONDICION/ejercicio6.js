let numero = parseInt( prompt("ingrese nuemero"));

if (numero % 5 === 0){

    alert (`El numero ${numero} es dibisible entre 5`);
}
    else {
        alert(`El numero ${numero} no es diblisible entre 5`)
    }
     
